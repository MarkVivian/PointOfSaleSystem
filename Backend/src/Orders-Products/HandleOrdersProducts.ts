import Express from "express"

const HandleOrdersProducts = Express.Router()

HandleOrdersProducts.get("/GetData", (req, res)=>{
    
})

HandleOrdersProducts.post("/UpdateData", (req, res)=>{
    
})

export default HandleOrdersProducts;